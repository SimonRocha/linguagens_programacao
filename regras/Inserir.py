from linguagens_programacao.modelo import Paciente
from linguagens_programacao.modelo.dao import PacienteDAO

def inserirPaciente(nome, documento, dt_nascimento, dt_entrada):
    p = Paciente(999, nome, documento, dt_nascimento, dt_entrada)

    p = PacienteDAO.inserir(p)

    if (p != None):
        return 1
    else:
        return 0


def inserirAdministrador(nome, documento, dt_nascimento, dt_entrada):
    p = Paciente(999, nome, documento, dt_nascimento, dt_entrada)

    p = PacienteDAO.inserir(p)

    if (p != None):
        return 1
    else:
        return 0


def inserirPaciente(nome, documento, dt_nascimento, dt_entrada):
    p = Paciente(999, nome, documento, dt_nascimento, dt_entrada)

    p = PacienteDAO.inserir(p)

    if (p != None):
        return 1
    else:
        return 0


def inserirPaciente(nome, documento, dt_nascimento, dt_entrada):
    p = Paciente(999, nome, documento, dt_nascimento, dt_entrada)

    p = PacienteDAO.inserir(p)

    if (p != None):
        return 1
    else:
        return 0


def inserirPaciente(nome, documento, dt_nascimento, dt_entrada):
    p = Paciente(999, nome, documento, dt_nascimento, dt_entrada)

    p = PacienteDAO.inserir(p)

    if (p != None):
        return 1
    else:
        return 0


def inserirPaciente(nome, documento, dt_nascimento, dt_entrada):
    p = Paciente(999, nome, documento, dt_nascimento, dt_entrada)

    p = PacienteDAO.inserir(p)

    if (p != None):
        return 1
    else:
        return 0